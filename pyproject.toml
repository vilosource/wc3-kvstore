[tool.poetry]
name = "wc3-kvstore"
version = "0.1.0"
packages = [{ include = "kvstore" }]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
flask = ">=3.0,<4.0"
requests = ">=2.31,<3.0"

[build-system]
requires = ["poetry-core>=1.5.0"]
build-backend = "poetry.core.masonry.api"